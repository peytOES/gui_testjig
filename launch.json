{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Jaguar Production Debug",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/jaguar_production.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
